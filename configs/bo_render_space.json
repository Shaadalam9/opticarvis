{
  "space_id": "bo_render_space_v3_four_parameter",
  "description": "Four parameter preference BO space with a configurable pairwise comparison budget. Contour thickness and label scale remain fixed renderer settings.",
  "model_encoding": {
    "continuous": "normalised to [0, 1]",
    "palette_id": "one_hot_categorical",
    "perceptual_spacing": "pending literature review and pilot validation"
  },
  "parameters": {
    "mask_alpha": {
      "type": "float",
      "min": 0.0,
      "max": 0.7,
      "default": 0.14,
      "meaning": "0 hides the target mask; higher values increase target mask opacity."
    },
    "trajectory_alpha": {
      "type": "float",
      "min": 0.0,
      "max": 1.0,
      "default": 0.55,
      "meaning": "0 hides the trajectory ribbon; higher values increase trajectory visibility."
    },
    "background_dim_alpha": {
      "type": "float",
      "min": 0.0,
      "max": 0.4,
      "default": 0.06,
      "meaning": "0 means no dimming; higher values dim irrelevant background context."
    },
    "palette_id": {
      "type": "categorical",
      "values": [0, 1, 2, 3],
      "default": 0,
      "meaning": "Selects one predefined colour palette. Raw RGB is not optimised."
    }
  }
}
